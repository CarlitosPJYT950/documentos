#include <stdio.h>
int main(void){
    
    char str[] = "Hello World!";
    char *ptr = str;
    
    printf("Caracteres de la cadena: ");
    
    while(*ptr != '\0'){
        printf("%c", *ptr);
        ptr++;
    }
    
    return 0;
}