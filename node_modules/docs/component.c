#include <stdio.h>
#include <stdbool.h>

int main() {
    bool llueve = true; // Simulamos que está lloviendo

    if (llueve) {printf("Llevar paraguas");} // Tenemos una sombrilla
    else {printf("Llevar sombrilla");} // No tenemos una sombrilla

    return 0;
}