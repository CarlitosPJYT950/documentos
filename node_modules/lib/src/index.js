let days = ["Lunes", "Martes", "Miércoles", "Jueves", "Viernes", "Sábado", "Domingo"]
function getDay(n) {
    // body...
    if(n < 1 || n > 7){throw new Error("Out of range")}
    return days[n - 1]
}