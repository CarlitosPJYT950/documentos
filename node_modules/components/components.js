function fibonacci(limit) {
    const sequence = [0, 1];
    for (let i = 2; i < limit; i++) {sequence.push(sequence[i - 1] + sequence[i - 2]);}
    return sequence;
}

const limit = fibonacci(10);
console.log(limit);